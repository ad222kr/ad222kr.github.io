var app = angular.module('pub-map', ["ngRoute", "ngMap"]);


/*app.controller("MyController", function(NgMap) {
  NgMap.getMap().then(function(map) {
    console.log(map.getCenter());
    console.log("shapes", map.shapes);
  })
})

*/
